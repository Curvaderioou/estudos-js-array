// add um ou mais elementos no final da array e retorna o tamanho do novo array

const cores = ["azul", "vermelho", "verde", "preto"];
cores.push("roxo")

// ele retorna 5 e se dermos log nas cores, aparecem as cinco cores

//podemos armazenar o retorno tambem

const frutas = ["banana", "laranja", "pessego"]
const frutaPush = frutas.push("tamarindo")

